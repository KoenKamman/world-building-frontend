import { StatbarDirective } from './statbar.directive';

describe('StatbarDirective', () => {
  it('should create an instance', () => {
    const directive = new StatbarDirective();
    expect(directive).toBeTruthy();
  });
});
